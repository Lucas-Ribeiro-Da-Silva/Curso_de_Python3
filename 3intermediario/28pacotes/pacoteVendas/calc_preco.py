#obs: para criar um pacote o arquivo init.py não pode ser excluido da python package

def aumento(valor, porcentagem):
    r = valor + (valor * (porcentagem / 1000))
    return r

def reducao(valor, porcentagem):
    r = valor + (valor * (porcentagem / 1000))
    return r